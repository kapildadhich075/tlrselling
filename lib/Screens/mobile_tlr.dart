import 'package:flutter/material.dart';

class MobileTlr extends StatelessWidget {
  const MobileTlr({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.blue);
  }
}
