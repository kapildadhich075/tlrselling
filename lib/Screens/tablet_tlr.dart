import 'package:flutter/material.dart';

class TabletTlr extends StatelessWidget {
  const TabletTlr({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
